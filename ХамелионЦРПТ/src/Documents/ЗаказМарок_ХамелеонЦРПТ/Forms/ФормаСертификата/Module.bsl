
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	// Вставить содержимое обработчика.
	ПолеHTML_1=Параметры.Файл;
	//ДвоичныеДанные=ПолучитьИзВременногоХранилища(ПолеHTML_1);
	//ВремФайл=ПолучитьИмяВременногоФайла(".pdf");
	//ДвоичныеДанные.Записать(ВремФайл);
	//Файл=Новый Файл(ВремФайл);
	//Если Файл.Существует() Тогда
	//	ПолеHTML		=ВремФайл;
	//Конецесли;
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	// Вставить содержимое обработчика.
//	ДвоичныеДанные=ПолучитьИзВременногоХранилища(ПолеHTML_1);
//	ВремФайл=ПолучитьИмяВременногоФайла(".pdf");
//	ДвоичныеДанные.Записать(ВремФайл);
	Файл=Новый Файл(ПолеHTML_1);
	Если Файл.Существует() Тогда
		Если Конст_PDF() Тогда
			ЗапуститьПриложение(ПолеHTML_1);
			Отказ=Истина;
			
		Иначе
			ПолеHTML		=ПолеHTML_1;
		КонецЕсли;
	Конецесли;
КонецПроцедуры

&НаСервереБезКонтекста
Функция Конст_PDF()
	Возврат РегистрыСведений.НастройкиПрограммы_ХамелеонЦРПТ.Получить().ОткрытьPDFЧерезВнешнююПрограмму
КонецФункции