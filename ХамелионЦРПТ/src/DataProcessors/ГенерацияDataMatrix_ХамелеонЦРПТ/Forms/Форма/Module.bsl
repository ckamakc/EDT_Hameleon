
&НаКлиенте
Процедура Сгенерировать(Команда)
	
	Если ПроверитьЗаполнение() Тогда
		СгенерироватьНаСервере();
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура СгенерироватьНаСервере()
	
	Генерация = РеквизитФормыВЗначение("Объект");
	Генерация.Инициализировать();
	ДанныеКартинки = Генерация.Сгенерировать(ИсходнаяСтрока);
	Картинка = ПоместитьВоВременноеХранилище(ДанныеКартинки, УникальныйИдентификатор);
	
КонецПроцедуры
